apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.longhorn.configSize }}
  storageClassName: {{ .Values.longhorn.storageClassName | quote }}
