longhorn:
  storageClassName: longhorn
  dataSize: 200Mi

app-template:
  fullnameOverride: backrest
  controllers:
    main:
      containers:
        main:
          image:
            repository: garethgeorge/backrest
            tag: latest
          probes:
            liveness:
              enabled: false
            readiness:
              enabled: false
            startup:
              enabled: false
          env:
          - name: TZ
            value: Asia/Ho_Chi_Minh
          - name: BACKREST_PORT
            value: "0.0.0.0:9898"
          - name: BACKREST_CONFIG
            value: /data/config/config.json
          - name: BACKREST_DATA
            value: /data/data
          ports:
          - name: http
            containerPort: 9898
  service:
    main:
      enabled: true
      controller: main
      type: ClusterIP
      ports:
        http:
          enabled: true
          primary: true
          port: 9898
          protocol: HTTP
  ingress:
    main:
      enabled: true
      className: traefik
      annotations: {}
      hosts:
      - host: backrest.ashpex.net
        paths:
        - path: "/"
          pathType: Prefix
          service:
            identifier: main
            port: http
  persistence:
    main-data:
      enabled: true
      type: persistentVolumeClaim
      existingClaim: backrest-data
      advancedMounts:
        main:
          main:
          - path: "/data"
    main-cache:
      enabled: true
      type: emptyDir
      sizeLimit: 500Mi
      advancedMounts:
        main:
          main:
          - path: "/cache"
    main-hdd1:
      enabled: true
      type: hostPath
      hostPath: "/mnt/hdd1"
      hostPathType: Directory
      advancedMounts:
        main:
          main:
          - path: "/hdd1"
    main-rclone:
      enabled: true
      type: hostPath
      hostPath: "/home/ashpex/.config/rclone"
      hostPathType: Directory
      advancedMounts:
        main:
          main:
          - path: "/root/.config/rclone"
            readOnly: true
