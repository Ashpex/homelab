{{- $server := required "nfs.server is required" .Values.nfs.server -}}
{{- $volumes := list
  (dict "name" "archive" "path" "/mnt/hdd1/archive" "size" "1Gi")
  (dict "name" "library" "path" "/mnt/hdd1/media/library" "size" "1Gi")
-}}
{{- range $volume := $volumes }}
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: alist-{{ $volume.name }}-nfs
spec:
  capacity:
    storage: {{ $volume.size }}
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  mountOptions:
    - nfsvers=4.1
  nfs:
    server: {{ $server | quote }}
    path: {{ $volume.path | quote }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: alist-{{ $volume.name }}
  namespace: {{ $.Release.Namespace }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ $volume.size }}
  volumeName: alist-{{ $volume.name }}-nfs
  storageClassName: ""
{{- end }}
