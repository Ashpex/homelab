apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: daily-backup
  namespace: {{ .Release.Namespace }}
spec:
  cron: "0 2 * * *"
  task: backup
  retain: 3
  concurrency: 2
  groups:
    - default
